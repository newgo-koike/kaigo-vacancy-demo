# 介護施設空室検索システム 仕様書 v2.7

> システム名：介護施設空室検索システム  
> 運営：Meets Medical  
> 更新：2026-08-20  
> 現行ブランチ：main（本番稼働中）  
> Firebase プロジェクト：kaigo-link-dev-59bc5  
> 公開URL：https://kaigo-link-dev-59bc5.web.app

---

## 1. システム概要

| 項目 | 内容 |
|------|------|
| ターゲット | 病院ソーシャルワーカー・ケアマネ（検索側）/ 介護施設担当者（更新側） |
| 目的 | 退院患者の受け入れ先を素早く見つける |
| 認証 | Firebase Auth（メール/パスワード）・招待制（自己登録なし） |
| 最重要情報 | 空室数・入居条件・費用・連絡先 |
| 想定規模 | 豊中市・吹田市・茨木市 → 全国展開（1,000〜4,000件） |

---

## 2. ユーザーロール

| ロール | 説明 | アクセス可能ページ |
|--------|------|-----------------|
| `admin` | Meets Medical 管理者 | 全ページ |
| `hospital` | 病院・ケアマネ | 検索・施設詳細 |
| `facility` | 介護施設・法人担当者 | 法人ダッシュボード・空室更新 |

### ログイン入り口の分離

```
医療機関・ケアマネ → kaigo-login.html
介護施設・法人    → kaigo-facility-login.html
```

### ログイン状態の保持

Firebase Auth の `Persistence.LOCAL` を設定しているため、**タブ／ブラウザを閉じてもログイン状態は維持される**（ブラウザのパスワード記憶ではなくシステム側で保持）。再入力が必要になるのは次の場合のみ。

- 自分で「ログアウト」を押した
- 別のPC・別のブラウザ・シークレットウィンドウで開いた
- ブラウザの閲覧履歴（Cookie・サイトデータ）を削除した

未ログインの状態で `kaigo-search.html` を直接開いた場合は `kaigo-login.html` へ自動リダイレクトするため、病院への案内URLはどちらでも動作する。
ログイン後、ヘッダー右上には「担当者名 さん」（担当者アカウント）または「病院名」（病院アカウント）を表示する。

---

## 3. データ構造（Firestore）

### corporations（法人・運営母体）
```
corporations/{corpId}
  name          : "社会福祉法人○○会"
  contactEmail  : "contact@corp.jp"
  createdAt     : Timestamp
```

### facilities（施設）
```
facilities/{facId}
  corporationId : "corpId"  ← 法人紐づけ
  name          : "○○介護センター"
  type          : "特養" | "老健" | "グループホーム" | "サ高住" | "介護付有料" | "住宅型有料"
  prefecture    : "大阪府"
  city          : "豊中市"
  area          : "庄内"
  address       : "豊中市..."
  stationLine   : "阪急宝塚線、大阪メトロ谷町線"  ← 複数路線「、」区切り
  station       : "服部天神駅"
  stationDistance: "徒歩5分"
  vacancy       : 3          ← 空室数
  vacancyNotes  : ""
  updatedAt     : Timestamp
  initialFee    : true/false
  initialFeeAmount: 0        ← 万円
  rent          : 8.0        ← 万円
  managementFee : 2.0
  mealFee       : 4.5
  otherFee      : 0
  monthlyTotal  : 14.5       ← 自動計算
  seikatsuHogo  : "可"|"相談可"|"不可"
  mimonHosho    : "可"|"相談可"|"不可"
  kaigoNintei   : "なし"|"相談可"|"不可"
  features      : ["看取り", "個室あり", ...]
  photoUrl      : ""
  plan          : "free"|"paid"
  approved      : true/false
```

### users（ユーザー）
```
users/{uid}
  name          : "田中 花子"
  email         : "..."
  role          : "admin"|"hospital"|"facility"
  corporationId : "corpId"   ← facilityロールのみ
  createdAt     : Timestamp
```

---

## 4. ページ構成

| ファイル | 役割 | アクセス権限 | 状態 |
|---------|------|------------|------|
| kaigo-login.html | 医療機関・ケアマネ ログイン | 全員 | ✅ 完了 |
| kaigo-facility-login.html | 介護施設・法人 ログイン | 全員 | ✅ 完了 |
| kaigo-search.html | 施設検索（エメラルド緑UIリニューアル済） | hospital / admin | ✅ 完了 |
| kaigo-facility-view.html | 施設詳細（Noto Sans JP・費用内訳整備済） | hospital / admin | ✅ 完了 |
| kaigo-facility-dashboard.html | 法人ダッシュボード（施設一覧） | facility / admin | ✅ 完了 |
| kaigo-manage.html | 空室更新（Firebase Auth認証ガード追加済） | facility / admin | ✅ 完了 |
| kaigo-register.html | 施設登録・編集（路線名フィールド追加済） | admin | ✅ 完了（法人紐づけ未対応） |
| kaigo-master.html | 管理者ダッシュボード（QRダウンロード追加済） | admin | ✅ 完了 |
| kaigo-delivery.html | 納品書 v1 | admin | ✅ 完了 |
| kaigo-delivery-v2.html | 納品書 v2（Inter+Noto・グラデーションヘッダー） | admin | ✅ 完了 |
| kaigo-import-tool.html | 施設データ一括インポート（158施設対応） | admin | ✅ 完了 |
| kaigo-csv-import.html | CSVインポートツール | admin | ✅ 完了 |
| kaigo-hearing-sheet.html | ヒアリングシート | admin | ✅ 完了 |
| kaigo-firstsetup.html | 初回セットアップ（廃止予定） | admin | → master.htmlに統合済 |

---

## 5. 画面仕様

### 5-1. 検索画面（kaigo-search.html）

3カラムレイアウト：

```
┌──────────────┬──────────────┬──────────────────────────────┐
│ 左サイドバー  │ タグ列        │ 施設カード一覧               │
│ [この条件で  │ こだわり・特徴 │ ソートバー（更新順/月額安/高） │
│  検索] ボタン │              │                              │
│              │ 看護・医療体制 │ [写真][施設情報][費用][空室数] │
│ 場所         │              │ [写真][施設情報][費用][空室数] │
│ 費用         │              │ ...                          │
│ 空室状況     │              │                              │
│ 施設種別     │              │                              │
│ 入居条件     │              │                              │
└──────────────┴──────────────┴──────────────────────────────┘
```

**施設カード（横並びリスト形式）**

```
┌──────────────────────────────────────────────────────────┐
│ [写真] │ 【特養】○○介護センター        │入居時│月額  │ 3室 │
│       │ 🏙 豊中市 🚉 服部天神駅5分     │ 0万  │14.5万│空き │
│       │ 生活保護:可 身元保証:相談可    │      │      │本日 │
└──────────────────────────────────────────────────────────┘
```

**検索ロジック**
1. 「検索」ボタン押下
2. Firestore: `where('prefecture','==',pref)` で取得（都道府県未選択時は全件）
3. クライアント側で市区町村・駅名・種別・費用・条件・タグをフィルタ
4. クライアント側でソート（更新順/月額安い順/高い順）

**駅名検索（v2.5新設・v2.6で「車で約15分圏」に拡張・v2.7でフリー検索窓に統合）**

検索窓は**フリー検索の1つだけ**（v2.7〜、兵頭さん要望）。入力が駅名かどうかを自動判定し、
駅名なら**その駅から車で約15分圏内の施設をすべて**駅から近い順に表示、駅名でなければ
従来どおり施設名・住所などのフリー検索として動く。
施設データに最寄駅として登録があるかどうかに関係なく、地理的な距離で判定する。

| 項目 | 仕様 |
|------|------|
| 入力欄 | フリー検索窓（`id="q-station"`）に統合。専用欄 `q-eki` は v2.7 で廃止 |
| モード判定 | 正規化した入力が「登録駅名と完全一致」または「駅APIで該当あり」なら駅モード、それ以外はフリー検索。絞り込みチップに「最寄駅: 〇〇駅（車15分圏）」/「検索: 〇〇」とモードが表示される |
| 判定例 | 「豊中駅」「豊中」→ 駅モード（214件）／「豊中市」→ 駅ではないのでフリー検索（154件）／「さくら」→ フリー検索 |
| 入力の正規化 | 空白（半角・全角）を除去し、末尾の「駅」を取り除く。「豊中駅」「豊中」は同じ検索になる |
| 駅の座標 | HeartRails Express API（無料・キー不要・CORS可）から検索時に取得しキャッシュ。同名駅対策で大阪・兵庫・京都・奈良の4府県に限定。**施設データに未登録の駅（例: 江坂）も指定できる** |
| 施設の座標 | `public/kaigo-geo-data.js`（住所→[緯度,経度]、国土地理院APIで事前生成、26KB）。生成は `tools/build_geo_data.py`。**データ再インポート後は必ず再生成する** |
| 「車で15分」の定義 | 実走行ルートAPI（有料）は使わない近似。直線距離 × 道のり係数1.3 ÷ 平均時速25km/h ≦ 15分（＝直線約4.8km圏）。定数 `EKI_DRIVE_LIMIT_MIN` / `EKI_SPEED_KMH` / `EKI_ROAD_FACTOR` で調整可 |
| 該当条件 | 「車15分圏内」または「登録最寄駅（`stations[].name`）の一致」のどちらか |
| 並び順 | 駅からの直線距離の近い順。座標が無い施設は登録駅の徒歩分数を80m/分で換算して混ぜる。安定ソートのため同距離内は並び替えセレクトの順序を保つ |
| カード表示 | 登録駅で徒歩分数があれば「〇〇駅 徒歩N分」、無ければ「〇〇駅 車で約N分」（推定） |
| フォールバック | 駅座標APIが落ちている・該当駅が無いときは、従来どおり登録駅の一致だけで絞り込む（エラーにしない） |
| 他条件との関係 | 種別・費用・市区町村など既存フィルタとAND。絞り込みチップ「最寄駅: 〇〇」から個別解除可 |
| 保存・復元 | 検索条件保存（localStorage）・施設詳細から戻ったときの結果復元（sessionStorage）に含まれる。検索ログは駅モードで動いたときだけ `searchLogs.conditions.nearestStation` に正規化駅名が入る。2窓時代（v2.5〜2.6）の保存条件も読み込み互換あり |

バス停（`buses[]`）は対象外（従来どおりフリー検索でヒットする）。

**所在地地図（v2.6〜・施設詳細ページ）**

`kaigo-facility-view.html` の「アクセス・所在地」カードに地図を表示する。
Leaflet（OSS）＋国土地理院タイルの構成で、**APIキー不要・従量課金なし**（Google Mapsは未使用）。
座標は `kaigo-geo-data.js` から引き、無い施設は地図を非表示にする。地図下に「Google マップで
開く（経路検索）」リンクを設置（リンクのみなので無料）。印刷時は地図を出さない。

### 5-1-2. 資料出力（印刷・PDF）

出し方は2ルート。どちらも別タブでA4シートを開き、右上の「🖨 印刷・PDF」ボタンからブラウザの印刷画面で 紙印刷 / PDF保存 する。

| ルート | 操作 | 出力 |
|--------|------|------|
| 個別 | 検索結果カードをクリック → 施設詳細 → 右上「🖨 印刷・PDF」 | A4縦・1施設1枚 |
| 比較 | 検索結果カード左上のチェック → 右下「📄 資料出力」 | A4横・横並び比較表 |

**共通ルール**

- 「資料出力」で1件だけ選んだ場合は、個別ルートとまったく同じA4縦シートを出す（共通ロジック `public/kaigo-print.js` の `printFacilitySheet()`）。
- 費用の目安データは施設種別ごとに `public/kaigo-cost-data.js` に集約し、**施設詳細・単票資料・比較表の3か所が同じ数値・同じ文言を参照する**。
- 月々の合計費用（目安）＝ 施設費用 ＋ 介護費用 ＋ 医療費 ＋ その他生活費。
  **小数第1位まで表示する**（例：8〜9.9万円）。整数に丸めると他画面と食い違うため四捨五入しない。桁数が増える施設では数字のフォントサイズを自動で縮めてA4幅に収める。
- 「印刷・PDF」ボタンは画面右上に固定表示（`position:fixed`）。`@media print` で非表示にしているため紙・PDFには写らない。

### 5-2. 法人ダッシュボード（kaigo-facility-dashboard.html）

```
ヘッダー: [法人名] 施設管理 | [ユーザー名] [ログアウト]

サマリー: [管理施設数] [空室あり] [14日以上未更新]

施設一覧:
┌─────────────────────────────────────────────────┐
│ 【特養】○○介護センター  豊中市  │ 本日更新 │ 3室 │[更新]│
│ 【老健】△△ホーム       吹田市  │ 5日前   │ 満室│[更新]│
└─────────────────────────────────────────────────┘
```

- `corporationId` で施設を絞り込み
- 「更新」ボタン → `kaigo-manage.html?id={facId}`

### 5-3. 管理者ダッシュボード（kaigo-master.html）

3タブ構成：

| タブ | 内容 |
|------|------|
| 施設管理 | 全施設テーブル・QRコード・掲載承認・プラン切替 |
| ユーザー管理 | アカウント発行（法人選択）・登録済みユーザー一覧 |
| 検索履歴 | 準備中 |

---

## 6. 実装ステータス

### ✅ 完了済み

| タスク | 完了日 |
|--------|--------|
| Firebase Authentication（3ロール） | 〜6/17 |
| 検索UI（3カラム・横並びカード・費用2パネル） | 〜6/17 |
| 施設カードに入居条件・費用・ソート機能 | 〜6/17 |
| Noto Sans JP フォント導入 | 〜6/17 |
| ログイン画面の分離（医療機関/施設） | 6/18 |
| 法人（corporation）データ構造設計・実装 | 6/18 |
| 法人ダッシュボード（kaigo-facility-dashboard.html） | 6/18 |
| 管理者: 法人登録・ユーザー管理・3タブ化 | 6/18 |
| 管理者ダッシュボード QRコードダウンロードボタン | 6/18 |
| kaigo-manage.html Firebase Auth認証ガード追加 | 6/18 |
| kaigo-register.html 路線名（stationLine）フィールド追加 | 6/18 |
| 施設詳細ページ（kaigo-facility-view.html）整備 | 6/19 |
| 検索UIカラースキームリニューアル（エメラルド緑） | 6/19 |
| 納品書 v2 デザイン全面刷新（kaigo-delivery-v2.html） | 6/19 |
| 施設データ一括インポートツール（158施設・kaigo-import-tool.html） | 6/19 |
| CSVインポートツール（kaigo-csv-import.html） | 6/19 |
| main マージ＆Firebase 本番デプロイ | 6/19 |
| 検索の都道府県・市区町村を複数選択（チェックボックス）化 | 7/29 |
| 費用表記の統一・チェックボックス／印刷ボタンの拡大（下記 変更履歴 参照） | 8/20 |
| 最寄駅から検索（駅名正規化・徒歩分数の近い順表示） | 8/24 |
| 最寄駅検索を「車で約15分圏」に拡張（座標ベース・未登録駅対応） | 8/24 |
| 施設詳細に所在地地図（Leaflet＋国土地理院タイル・無課金） | 8/24 |
| 検索窓を1つに統合（駅名/フリーを自動判別） | 8/24 |

### 🔲 残タスク

| タスク | 優先度 |
|--------|--------|
| kaigo-register.html に法人紐づけ追加 | **高 ← 次** |
| GitHub Actions による自動デプロイ設定 | 中 |
| 検索履歴のFirestore保存 | 中 |
| 地図表示（Google Maps連携） | 低 |
| kaigo-search.html の Tailwind 化 | 低 |

---

## 7. 開発体制

| 役割 | 担当 |
|------|------|
| 発注・データ収集 | 兵頭さん |
| フロントエンド開発 | 西島くん（GitHub: kechijima） |
| AI実装サポート | Claude Code |
| リポジトリ | https://github.com/newgo-koike/kaigo-vacancy-demo |

---

## 8. 変更履歴

### 2026-08-24（v2.7）

**検索窓を1つに統合**（兵頭さん要望「検索窓は1つがいい」「フリー検索に豊中駅と入れたら出るように」）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 「最寄駅から検索」欄を廃止 | フリー検索窓1つに統合。ラベルは「フリー検索（施設名・駅名・住所など）」のまま |
| 2 | 駅名の自動判定 | 入力が駅名（登録駅と完全一致 or 駅APIで該当）なら自動で車15分圏検索に切替。「豊中駅」でも「豊中」でもOK |
| 3 | モードの可視化 | 絞り込みチップで「最寄駅: 豊中駅（車15分圏）」/「検索: 〇〇」を出し分け |
| 4 | 互換対応 | 2窓時代に保存した検索条件も読み込める |

**背景**：v2.6まで「豊中駅」とフリー検索に入れても0件だった（内部の駅名が「豊中」表記のため
「駅」付きでは部分一致しない）。統合により、どちらの窓に入れるか迷う問題と「駅」表記ゆれの
問題が同時に解消した。検証：豊中駅/豊中=駅モード214件・豊中市=フリー154件・江坂=駅モード
197件・さくら=フリー13件・存在しない駅名=フリー0件（エラーなし）。

### 2026-08-24（v2.6）

**最寄駅検索を「車で約15分圏」に拡張＋施設詳細に所在地地図**（兵頭さん依頼）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 車15分圏の距離検索 | 駅名から座標を引き（HeartRails API）、施設座標（事前ジオコーディング）との直線距離で判定。登録最寄駅に関係なく圏内の施設をすべて表示。「豊中駅」17件 → 214件 |
| 2 | 未登録駅にも対応 | データに最寄駅として登録が無い駅（例: 江坂）でも検索可能に |
| 3 | 施設座標データ | `public/kaigo-geo-data.js` を新設（476件中463住所、国土地理院APIで生成）。生成スクリプト `tools/build_geo_data.py` |
| 4 | カード表示 | 徒歩分数があれば「徒歩N分」、無ければ直線距離からの推定で「車で約N分」 |
| 5 | 施設詳細に地図 | Leaflet＋国土地理院タイル（無料・キー不要・従量課金なし）＋Googleマップ経路リンク |

**動作のポイント（誤解しやすい点）**：施設側に最寄駅の登録があるかどうかは判定に関係ない。
駅名を入れれば「車で約15分圏内」の施設は登録の有無にかかわらずすべて表示される。
駅側も同様で、どの施設にも登録されていない駅名（例: 江坂）を指定できる。

**検証実績（2026-08-24・実データ476件で確認）**

| 検索 | 結果 | 備考 |
|------|------|------|
| 豊中駅 / 豊中（表記ゆれ） | 214件・同一結果 | うち登録最寄駅の一致は17件のみ。残り197件は距離だけでヒット |
| 江坂（施設データに登録なしの駅） | 197件 | 吹田市・豊中市の圏内施設が距離順に表示 |
| 梅田（対象5市の外の駅） | 8件 | 豊中市南部の圏内分のみ。距離判定が正しく効いている |
| 存在しない駅名 | 0件 | エラーにならずフォールバック動作 |
| 距離順ソート | 0.13km→4.79km | 上限（直線約4.8km）内で近い順を確認 |

**費用について**：地図・座標・駅APIはすべて無料構成（国土地理院タイル・国土地理院ジオコーディング・
HeartRails・Leaflet）。Google Maps API は使っていないため、アクセスが増えても従量課金は発生しない。

**精度の注意**：「車で約15分」は直線距離×係数1.3を時速25km/hで換算した推定値（渋滞・信号は考慮しない）。
実ルートが必要になったら Google Distance Matrix 等（有料）への差し替えを検討する。

### 2026-08-24（v2.5）

**最寄駅から検索を新設**（兵頭さん依頼）。対象は `public/kaigo-search.html` のみ。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 「最寄駅から検索」入力欄を追加 | フリー検索の下に専用欄。「〇〇駅」でも「〇〇」でも同一の検索として扱う |
| 2 | 徒歩分数の近い順ソート | 検索した駅の徒歩分数が小さい施設から表示。分数未登録は末尾 |
| 3 | カードの最寄駅タグ切替 | 最寄駅検索中は「検索した駅＋徒歩分数」を表示 |
| 4 | 付帯対応 | 絞り込みチップ・検索条件保存/復元・詳細から戻ったときの結果復元・検索ログ（`nearestStation`）に対応 |

**背景**：従来はフリー検索しかなく、「豊中」と入れると豊中市（住所）と豊中駅（駅名）が混ざってヒットし、
駅起点で近い施設を探す使い方ができなかった。最寄駅検索は照合対象を駅データに限定し、距離順で返す。
詳細仕様は「5-1. 検索画面」の**最寄駅から検索**を参照。

### 2026-08-20（v2.4）

兵頭さんからの指摘3点に対応。対象は `public/` 配下の3ファイル。

| # | 変更点 | 変更前 → 変更後 | ファイル |
|---|--------|----------------|---------|
| 1 | **月額目安の表記を統一** | `8〜10万円`（整数に四捨五入）→ `8〜9.9万円`（小数第1位） | `kaigo-print.js` |
| 2 | 検索結果カードの選択チェックボックスを拡大 | 20px → 28px（白い台紙＋黒枠＋影を追加） | `kaigo-search.html` |
| 3 | 「印刷・PDF」ボタンを拡大 | 施設詳細：13px → 17px ／ 資料出力タブ：11px角ボタン → 17px丸ボタン＋画面右上に固定 | `kaigo-facility-view.html`, `kaigo-search.html`, `kaigo-print.js` |

**1の背景**：単票資料（`kaigo-print.js`）だけがヒーロー金額を `Math.round()` で整数に丸めており、9.9 → 10 に繰り上がっていた。施設詳細ページ・比較表は当初から小数第1位表示だったため、同じ施設で表記が食い違っていた。丸め処理を廃止し、桁数に応じてフォントサイズを可変（82pt / 68pt / 58pt）にしてA4幅に収まるようにした。

**3の補足**：資料シートの印刷ボタンは `position:fixed` に変更したため、長い比較表を下までスクロールしても常に押せる。ボタン拡大に伴い施設詳細ページのヘッダー高さを 58px → 68px に調整。
