# 介護施設空室検索システム 仕様書 v2.17.3

> システム名：介護施設空室検索システム  
> 運営：Meets Medical  
> 更新：2026-08-20  
> 現行ブランチ：main（本番稼働中）  
> Firebase プロジェクト：kaigo-link-dev-59bc5  
> 公開URL：https://kaigo.meetsmedical.com（旧 https://kaigo-link-dev-59bc5.web.app も並行して有効）

---

## 1. システム概要

| 項目 | 内容 |
|------|------|
| ターゲット | 病院ソーシャルワーカー・ケアマネ（検索側）/ 介護施設担当者（更新側） |
| 目的 | 退院患者の受け入れ先を素早く見つける |
| 認証 | Firebase Auth（メール/パスワード）・招待制（自己登録なし） |
| 最重要情報 | 空室数・入居条件・費用・連絡先 |
| 想定規模 | 大阪7市＋大阪市3区（豊中・吹田・茨木・池田・箕面・摂津・高槻／淀川区・東淀川区・西淀川区）＋兵庫4市（尼崎・伊丹・川西・宝塚）1,169件 → 全国展開 |

---

## 1-2. 公開URL・ドメイン（v2.11〜）

| 項目 | 内容 |
|------|------|
| 正式URL | `https://kaigo.meetsmedical.com`（2026-09-13 開通） |
| 旧URL | `https://kaigo-link-dev-59bc5.web.app`（並行して有効。案内済みの病院はそのまま使える） |
| 仕組み | Firebase Hosting のカスタムドメイン。DNS は **CNAME `kaigo` → `kaigo-link-dev-59bc5.web.app` の1本だけ**。証明書検証用 TXT `_acme-challenge.kaigo` は代替手段で、CNAMEが向けば Firebase が HTTP 検証で自動発行・自動更新する（GMOレンタルサーバーのDNSはホスト名にアンダースコアを使えないため TXT は入れていない。実際に TXT なしで発行済み）。SSL証明書は Google Trust Services 発行 |
| DNSの置き場所 | meetsmedical.com の委任先NSは `dns01/dns02.gmoserver.jp`（GMOレンタルサーバー側）。**お名前.com Navi の「DNSレコード設定」は参照されない**ので、レコードはレンタルサーバーのコントロールパネル側に入れる（初回はNavi側に入れて反映せず、切り分けに半日要した） |
| 認証 | Firebase Auth の承認済みドメインに `kaigo.meetsmedical.com` を追加済み（未登録だとログインが失敗する） |
| 他ツールの追加 | 同じ手順で `xxx.meetsmedical.com` を増やせる（Hosting REST API で customDomain 作成 → レコード発行 → レンタルサーバー側DNSへ追加 → 承認済みドメイン追加） |

## 2. ユーザーロール

| ロール | 説明 | アクセス可能ページ |
|--------|------|-----------------|
| `admin` | Meets Medical 管理者 | 全ページ |
| `hospital` | 病院・ケアマネ | 検索・施設詳細 |
| `facility` | 介護施設・法人担当者 | 法人ダッシュボード・空室更新 |

### ログイン入り口の分離

```
医療機関・ケアマネ → kaigo-login.html
介護施設・法人    → kaigo-facility-login.html
```

### ログイン状態の保持

Firebase Auth の `Persistence.LOCAL` を設定しているため、**タブ／ブラウザを閉じてもログイン状態は維持される**（ブラウザのパスワード記憶ではなくシステム側で保持）。再入力が必要になるのは次の場合のみ。

- 自分で「ログアウト」を押した
- 別のPC・別のブラウザ・シークレットウィンドウで開いた
- ブラウザの閲覧履歴（Cookie・サイトデータ）を削除した

未ログインの状態で `kaigo-search.html` を直接開いた場合は `kaigo-login.html` へ自動リダイレクトするため、病院への案内URLはどちらでも動作する。
ログイン後、ヘッダー右上には「担当者名 さん」（担当者アカウント）または「病院名」（病院アカウント）を表示する。

---

## 3. データ構造（Firestore）

### corporations（法人・運営母体）
```
corporations/{corpId}
  name          : "社会福祉法人○○会"
  contactEmail  : "contact@corp.jp"
  createdAt     : Timestamp
```

### facilities（施設）
```
facilities/{facId}
  corporationId : "corpId"  ← 法人紐づけ
  name          : "○○介護センター"
  type          : "特養" | "老健" | "グループホーム" | "サ高住" | "介護付有料" | "住宅型有料"
  prefecture    : "大阪府"
  city          : "豊中市"
  area          : "庄内"
  address       : "豊中市..."
  stationLine   : "阪急宝塚線、大阪メトロ谷町線"  ← 複数路線「、」区切り
  station       : "服部天神駅"
  stationDistance: "徒歩5分"
  vacancy       : 3          ← 空室数
  vacancyNotes  : ""
  updatedAt     : Timestamp
  initialFee    : true/false
  initialFeeAmount: 0        ← 万円
  rent          : 8.0        ← 万円
  managementFee : 2.0
  mealFee       : 4.5
  otherFee      : 0
  monthlyTotal  : 14.5       ← 自動計算
  seikatsuHogo  : "可"|"相談可"|"不可"
  mimonHosho    : "可"|"相談可"|"不可"
  kaigoNintei   : "なし"|"相談可"|"不可"
  features      : ["看取り", "個室あり", ...]
  photoUrl      : ""
  plan          : "free"|"paid"
  approved      : true/false
```

### users（ユーザー）
```
users/{uid}
  name          : "田中 花子"
  email         : "..."
  role          : "admin"|"hospital"|"facility"
  corporationId : "corpId"   ← facilityロールのみ
  createdAt     : Timestamp
```

---

## 4. ページ構成

| ファイル | 役割 | アクセス権限 | 状態 |
|---------|------|------------|------|
| kaigo-login.html | 医療機関・ケアマネ ログイン | 全員 | ✅ 完了 |
| kaigo-facility-login.html | 介護施設・法人 ログイン | 全員 | ✅ 完了 |
| kaigo-search.html | 施設検索（エメラルド緑UIリニューアル済） | hospital / admin | ✅ 完了 |
| kaigo-facility-view.html | 施設詳細（Noto Sans JP・費用内訳整備済） | hospital / admin | ✅ 完了 |
| kaigo-facility-dashboard.html | 法人ダッシュボード（施設一覧） | facility / admin | ✅ 完了 |
| kaigo-manage.html | 空室更新（Firebase Auth認証ガード追加済） | facility / admin | ✅ 完了 |
| kaigo-register.html | 施設登録・編集（路線名フィールド追加済） | admin | ✅ 完了（法人紐づけ未対応） |
| kaigo-master.html | 管理者ダッシュボード（QRダウンロード追加済） | admin | ✅ 完了 |
| kaigo-delivery.html | 納品書 v1 | admin | ✅ 完了 |
| kaigo-delivery-v2.html | 納品書 v2（Inter+Noto・グラデーションヘッダー） | admin | ✅ 完了 |
| kaigo-import-tool.html | 施設データ一括インポート（158施設対応） | admin | ✅ 完了 |
| kaigo-csv-import.html | CSVインポートツール | admin | ✅ 完了 |
| kaigo-hearing-sheet.html | ヒアリングシート | admin | ✅ 完了 |
| kaigo-firstsetup.html | 初回セットアップ（廃止予定） | admin | → master.htmlに統合済 |

---

## 5. 画面仕様

### 5-1. 検索画面（kaigo-search.html）

3カラムレイアウト：

```
┌──────────────┬──────────────┬──────────────────────────────┐
│ 左サイドバー  │ タグ列        │ 施設カード一覧               │
│ [この条件で  │ こだわり・特徴 │ ソートバー（更新順/月額安/高） │
│  検索] ボタン │              │                              │
│              │ 看護・医療体制 │ [写真][施設情報][費用][空室数] │
│ 場所         │              │ [写真][施設情報][費用][空室数] │
│ 費用         │              │ ...                          │
│ 空室状況     │              │                              │
│ 施設種別     │              │                              │
│ 入居条件     │              │                              │
└──────────────┴──────────────┴──────────────────────────────┘
```

**施設カード（横並びリスト形式）**

```
┌──────────────────────────────────────────────────────────┐
│ [写真] │ 【特養】○○介護センター        │入居時│月額  │ 3室 │
│       │ 🏙 豊中市 🚉 服部天神駅5分     │ 0万  │14.5万│空き │
│       │ 生活保護:可 身元保証:相談可    │      │      │本日 │
└──────────────────────────────────────────────────────────┘
```

**検索ロジック**
1. 「検索」ボタン押下
2. Firestore: `where('prefecture','==',pref)` で取得（都道府県未選択時は全件）
3. クライアント側で市区町村・駅名・種別・費用・条件・タグをフィルタ
4. クライアント側でソート（更新順/月額安い順/高い順/最寄駅から近い順）

**駅名検索（v2.5新設・v2.6で「車で約15分圏」に拡張・v2.7でフリー検索窓に統合）**

検索窓は**フリー検索の1つだけ**（v2.7〜、兵頭さん要望）。入力が駅名かどうかを自動判定し、
駅名なら**その駅から車で約15分圏内の施設をすべて**駅から近い順に表示、駅名でなければ
従来どおり施設名・住所などのフリー検索として動く。
施設データに最寄駅として登録があるかどうかに関係なく、地理的な距離で判定する。

| 項目 | 仕様 |
|------|------|
| 入力欄 | フリー検索窓（`id="q-station"`）に統合。専用欄 `q-eki` は v2.7 で廃止 |
| モード判定 | 正規化した入力が「登録駅名と完全一致」または「駅APIで該当あり」なら駅モード、それ以外はフリー検索。絞り込みチップに「最寄駅: 〇〇駅（車15分圏）」/「検索: 〇〇」とモードが表示される |
| 判定例 | 「豊中駅」「豊中」→ 駅モード（214件）／「豊中市」→ 駅ではないのでフリー検索（154件）／「さくら」→ フリー検索 |
| 入力の正規化 | 空白（半角・全角）を除去し、末尾の「駅」を取り除く。「豊中駅」「豊中」は同じ検索になる |
| 駅の座標 | 同梱の駅マスター `public/kaigo-eki-data.js`（OpenStreetMap由来・近畿4府県1,150駅・40KB、生成は `tools/build_eki_data.py`）を最優先で即時参照。マスターに無い駅名のみ HeartRails API に問い合わせる保険つき（2026-09-09のHeartRails全面停止を受けて外部API依存を解消）。**施設データに未登録の駅（例: 江坂）も指定できる** |
| 施設の座標 | `public/kaigo-geo-data.js`（住所→[緯度,経度]、国土地理院APIで事前生成、26KB）。生成は `tools/build_geo_data.py`。**データ再インポート後は必ず再生成する** |
| 「車で15分」の定義 | 実走行ルートAPI（有料）は使わない近似。直線距離 × 道のり係数1.3 ÷ 平均時速25km/h ≦ 15分（＝直線約4.8km圏）。定数 `EKI_DRIVE_LIMIT_MIN` / `EKI_SPEED_KMH` / `EKI_ROAD_FACTOR` で調整可 |
| 該当条件 | 「車15分圏内」または「登録最寄駅（`stations[].name`）の一致」のどちらか |
| 並び順 | 並べ替えが「更新が新しい順」（初期値）または「最寄駅から近い順」のときは登録駅の徒歩分数の近い順（80m/分で距離換算、分数未登録は末尾）。**「月額が安い順／高い順」を選んでいるときはその順を優先**し、駅名は絞り込みにだけ使う（v2.15.2）。安定ソートのため同距離内は並び替えセレクトの順序を保つ |
| カード表示 | 施設データの最寄駅表記をそのまま表示（「伊丹 徒歩約2分」「伊丹 バス約3分」）。v2.10で推定「車で約N分」は廃止（兵頭さん指摘：実データと食い違う） |
| フォールバック | 駅座標APIが落ちている・該当駅が無いときは、従来どおり登録駅の一致だけで絞り込む（エラーにしない） |
| 他条件との関係 | 種別・費用・市区町村など既存フィルタとAND。絞り込みチップ「最寄駅: 〇〇」から個別解除可 |
| 保存・復元 | 検索条件保存（localStorage）・施設詳細から戻ったときの結果復元（sessionStorage）に含まれる。検索ログは駅モードで動いたときだけ `searchLogs.conditions.nearestStation` に正規化駅名が入る。2窓時代（v2.5〜2.6）の保存条件も読み込み互換あり |

バス停（`buses[]`）は対象外（従来どおりフリー検索でヒットする）。

**所在地地図（v2.6〜・施設詳細ページ）**

`kaigo-facility-view.html` の「アクセス・所在地」カードに地図を表示する。
Leaflet（OSS）＋国土地理院タイルの構成で、**APIキー不要・従量課金なし**（Google Mapsは未使用）。
座標は `kaigo-geo-data.js` から引き、無い施設は地図を非表示にする。地図下に「Google マップで
開く（経路検索）」リンクを設置（リンクのみなので無料）。印刷時は地図を出さない。

### 5-1-2. 資料出力（印刷・PDF）

出し方は2ルート。どちらも別タブでA4シートを開き、右上の「🖨 印刷・PDF」ボタンからブラウザの印刷画面で 紙印刷 / PDF保存 する。

| ルート | 操作 | 出力 |
|--------|------|------|
| 個別 | 検索結果カードをクリック → 施設詳細 → 右上「🖨 印刷・PDF」 | A4縦・1施設1枚 |
| 比較 | 検索結果カード左上のチェック → 右下「📄 資料出力」 | A4横・横並び比較表 |

**共通ルール**

- 「資料出力」で1件だけ選んだ場合は、個別ルートとまったく同じA4縦シートを出す（共通ロジック `public/kaigo-print.js` の `printFacilitySheet()`）。
- 費用の目安データは施設種別ごとに `public/kaigo-cost-data.js` に集約し、**施設詳細・単票資料・比較表の3か所が同じ数値・同じ文言を参照する**。
- 月々の合計費用（目安）＝ 施設費用 ＋ 介護費用 ＋ 医療費 ＋ その他生活費。
  **小数第1位まで表示する**（例：8〜9.9万円）。整数に丸めると他画面と食い違うため四捨五入しない。桁数が増える施設では数字のフォントサイズを自動で縮めてA4幅に収める。
- 「印刷・PDF」ボタンは画面右上に固定表示（`position:fixed`）。`@media print` で非表示にしているため紙・PDFには写らない。

### 5-2. 法人ダッシュボード（kaigo-facility-dashboard.html）

```
ヘッダー: [法人名] 施設管理 | [ユーザー名] [ログアウト]

サマリー: [管理施設数] [空室あり] [14日以上未更新]

施設一覧:
┌─────────────────────────────────────────────────┐
│ 【特養】○○介護センター  豊中市  │ 本日更新 │ 3室 │[更新]│
│ 【老健】△△ホーム       吹田市  │ 5日前   │ 満室│[更新]│
└─────────────────────────────────────────────────┘
```

- `corporationId` で施設を絞り込み
- 「更新」ボタン → `kaigo-manage.html?id={facId}`

### 5-3. 管理者ダッシュボード（kaigo-master.html）

3タブ構成：

| タブ | 内容 |
|------|------|
| 施設管理 | 全施設テーブル・QRコード・掲載承認・プラン切替 |
| ユーザー管理 | アカウント発行（法人選択）・登録済みユーザー一覧 |
| 検索履歴 | 準備中 |

---

## 6. 実装ステータス

### ✅ 完了済み

| タスク | 完了日 |
|--------|--------|
| Firebase Authentication（3ロール） | 〜6/17 |
| 検索UI（3カラム・横並びカード・費用2パネル） | 〜6/17 |
| 施設カードに入居条件・費用・ソート機能 | 〜6/17 |
| Noto Sans JP フォント導入 | 〜6/17 |
| ログイン画面の分離（医療機関/施設） | 6/18 |
| 法人（corporation）データ構造設計・実装 | 6/18 |
| 法人ダッシュボード（kaigo-facility-dashboard.html） | 6/18 |
| 管理者: 法人登録・ユーザー管理・3タブ化 | 6/18 |
| 管理者ダッシュボード QRコードダウンロードボタン | 6/18 |
| kaigo-manage.html Firebase Auth認証ガード追加 | 6/18 |
| kaigo-register.html 路線名（stationLine）フィールド追加 | 6/18 |
| 施設詳細ページ（kaigo-facility-view.html）整備 | 6/19 |
| 検索UIカラースキームリニューアル（エメラルド緑） | 6/19 |
| 納品書 v2 デザイン全面刷新（kaigo-delivery-v2.html） | 6/19 |
| 施設データ一括インポートツール（158施設・kaigo-import-tool.html） | 6/19 |
| CSVインポートツール（kaigo-csv-import.html） | 6/19 |
| main マージ＆Firebase 本番デプロイ | 6/19 |
| 検索の都道府県・市区町村を複数選択（チェックボックス）化 | 7/29 |
| 費用表記の統一・チェックボックス／印刷ボタンの拡大（下記 変更履歴 参照） | 8/20 |
| 最寄駅から検索（駅名正規化・徒歩分数の近い順表示） | 8/24 |
| パンフレットPDF（管理画面の行にドロップで登録→検索カード・施設ページ・資料出力のQRに表示） | 9/23（Blaze切替後に本番反映） |
| 最寄駅検索を「車で約15分圏」に拡張（座標ベース・未登録駅対応） | 8/24 |
| 施設詳細に所在地地図（Leaflet＋国土地理院タイル・無課金） | 8/24 |
| 検索窓を1つに統合（駅名/フリーを自動判別） | 8/24 |

### 🔲 残タスク

| タスク | 優先度 |
|--------|--------|
| kaigo-register.html に法人紐づけ追加 | **高 ← 次** |
| GitHub Actions による自動デプロイ設定 | 中 |
| 検索履歴のFirestore保存 | 中 |
| 地図表示（Google Maps連携） | 低 |
| kaigo-search.html の Tailwind 化 | 低 |

---

## 7. 開発体制

| 役割 | 担当 |
|------|------|
| 発注・データ収集 | 兵頭さん |
| フロントエンド開発 | 西島くん（GitHub: kechijima） |
| AI実装サポート | Claude Code |
| リポジトリ | https://github.com/newgo-koike/kaigo-vacancy-demo |

---

## 8. 変更履歴

### 2026-09-24（v2.17.3）ドロップ登録が本番で動作確認済み

**解決**：cross-service ルールで Firestore を読むために権限が要るサービスアカウントは
`service-646624290677@gcp-sa-firebasestorage.iam.gserviceaccount.com`（Cloud Storage for Firebase のサービスエージェント）だった。
対話モードの `firebase deploy --only storage` が「Grant the new role? (Y/n)」で付与してくれる（`--non-interactive` は黙ってスキップ）。
付与の約2分後、小池さんの管理者アカウントで管理画面の行にPDFをドロップ → 行に表示・公開URLで200・索引と施設docに追記・
「×」で削除して Storage からも消える、まで本番で確認。管理者2名のトークンには admin クレームも付与済み（現行ルールでは未使用。
将来クレーム判定に切り替える場合の下地）。

### 2026-09-24（v2.17.2）Blaze 切替・Storage 稼働開始

**【解決済み・経緯として残す】管理画面からのドロップ登録は、IAM 付与が済むまで「権限なし（storage/unauthorized）」で失敗する。**
本番で小池さんの管理者アカウントから実際にドロップして判明。原因は `storage.rules` が Firestore の `users.role` を参照する
cross-service ルールで、Firebase Rules のサービスアカウント `service-646624290677@firebase-rules.iam.gserviceaccount.com` に
`roles/firebaserules.firestoreServiceAgent` が付与されていないため（`firebase deploy --only storage --non-interactive` は付与を
聞かずに通ってしまう）。対処はリポジトリ直下で対話モードの `firebase deploy --only storage` を実行し、付与の確認に y と答える
（または `gcloud projects add-iam-policy-binding` で同じ役割を付与）。付与後にもう一度ドロップして確認する。
- 2026-09-24 01:00頃 小池さんが gcloud で付与済み（IAM に反映されていることを確認）。ルールも再リリース。
  しかし 01:20 時点でも管理者のドロップは `storage/unauthorized` のまま。ルールの論理は Rules テストAPIで検証済み
  （Firestore 参照を模擬すると 管理者=許可／病院=拒否／PDF以外=拒否／未ログイン=拒否）。残る原因は cross-service の
  Firestore 参照が実行時にまだ通らないこと（IAM 反映の遅延、または Rules 側のキャッシュと推定）。
  次の一手: 時間をおいて再試行 → ダメなら管理者トークンにカスタムクレーム admin を付与し、ルールに
  `request.auth.token.admin == true` の判定を追加する（自動モードでは付与・ルール変更が止められるため小池さんが実行）。

- Firebase プランを **Blaze（従量課金）** に変更（小池さんがコンソールで実施。請求先アカウント 01DE89…）
- Storage の既定バケット `kaigo-link-dev-59bc5.firebasestorage.app` を **asia-northeast1** に作成
  （コンソールで迷ったため Firebase Storage API `projects.defaultBucket.create` で作成）。`storage.rules` をデプロイ
  （閲覧は公開、追加・削除は管理者のみ、PDF・20MBまで。未ログインの書き込みは 403 を確認）
- 箕面市11施設のPDFを Hosting 直置きから **Storage `brochures/<施設ID>/20260923-1.pdf` に移し替え**、施設docと索引を
  URL 指定 → path 指定に切替、一覧ファイルを再生成、Hosting から PDF を削除して再配信（旧URLは 404）。11本とも Storage から 200 を確認
- これで管理画面の行ドロップによる登録が本番で使える状態。`public/brochures/` は不要になったため削除（原本は兵頭さんからの受領分）

### 2026-09-23（v2.17.1）本番反映

**箕面市11施設のパンフレットPDFを先行掲載**（兵頭さんから受領。Blaze切替前のため Storage ではなく Hosting 直置き）。
- 置き場所: `public/brochures/<施設ID>/20260923-1.pdf`（合計100MB。GitHub には入れない＝`.gitignore`。正本は Dropbox 内のこのフォルダ）
- 登録: `tools/kaigo_brochure_link.py add <施設ID> <URL> --name ...` で施設docと索引 `meta/brochures` に URL 指定エントリとして登録。
  `kaigo-brochure.js` は `entry.url` があればそれを優先して開く（Storage 経由と同じ見え方）
- 反映済み: 検索ページ・施設ページ・資料出力（QR）・管理画面のPDF列・Firestore ルール（meta）。**storage.rules は Storage 未作成のため未デプロイ**
- 注意: Hosting の無料枠は配信 360MB/日。10MB前後のPDFなので1日30回程度の閲覧が上限の目安。Blaze 切替後は Storage（1GB/日）に移す
- 対象: アクティブライフ箕面／くらら箕面小野原／グランダ箕面／ささゆりの宿り／そんぽの家箕面／ハートケア箕面彩都／
  ファンライフ箕面／フォーユー箕面小野原／プレザンメゾン箕面／ユトリーム箕面桜ヶ丘／ラ・ソーラみのお牧落駅前

### 2026-09-23（v2.17）

**施設ごとのパンフレットPDF：管理画面にドロップするだけで病院側に反映**（兵頭さん要望）。
- **登録**：管理画面「施設管理」タブの施設の行にPDFをドロップ（またはPDF列をクリックして選択）。複数可、1ファイル20MBまで、PDF以外は拒否。
  進捗（%）を行内に表示し、完了するとその行にPDF名が出て「×」で削除できる。上部の集計に「PDF登録済」施設数を追加。
  施設編集ページ（kaigo-manage.html）にも管理者にだけ「パンフレット・資料（PDF）」欄を表示（同じくドロップ／削除）
- **病院側の表示**：検索カードに「PDF ○○」ボタン（カードを開かずPDFだけ別タブ）、施設ページに「パンフレット・資料（PDF）」カード、
  資料出力（単票）の2ページ目にPDFごとのQRコード（家族がスマホで読み取るとPDFが開く）、2件以上の比較表に「パンフレット」行
- **置き場所**：Firebase Storage `brochures/<施設ID>/<日時>_<ファイル名>.pdf`。閲覧は公開（URLを知っている人）、追加・削除は管理者のみ（`storage.rules`、
  役割は Firestore の users を参照）。URLはトークン無しの固定形式なので、一覧ファイル（静的JSON）にも安全に含められる
- **データ**：`facilities/<id>.brochures = [{name, path, size, at}]` が正本。検索ページが Firestore 読み取り1回で全施設分を引けるよう、
  索引 `meta/brochures`（施設ID → 同じ配列）を `public/kaigo-brochure.js` がアップロード・削除のたびに**同時更新**（batch）。
  これにより一覧ファイルを再生成しなくても、ドロップした直後から検索カードに出る（検索の読み取りは 0 → 1回/5分）。
  食い違いの確認・修復は `tools/kaigo_rebuild_brochure_index.py`
- **共通モジュール** `public/kaigo-brochure.js`（URL生成・検証・アップロード・削除・索引読み込み）を管理画面・施設編集・検索・施設ページ・資料出力で共有
- **前提（未実施）**：Storage は Blaze（従量課金）プラン専用のため、本番反映の前に (1) Firebase コンソールで Blaze へ切替（予算アラート推奨）
  (2) Storage を「開始」（バケット `kaigo-link-dev-59bc5.firebasestorage.app`）(3) `firebase deploy --only firestore:rules,storage,hosting`。
  費用目安：保存5GB・配信1GB/日まで無料枠（1,168施設×2MB＝約2.3GBで枠内）。超過しても保存1GBあたり月約4円

### 2026-09-18（v2.16）

**地図を印刷・PDFにも出す＋初期表示を2段階広域に**（兵頭さん要望）。
- 施設詳細ページの地図（Leaflet＋国土地理院）の初期ズームを 16 → **14** に（周辺の駅・幹線道路まで入る広さ）。
  値は `kaigo-print.js` の `KaigoPrint.MAP_ZOOM` 1か所で管理
- 単票資料（施設詳細の「印刷・PDF」／検索結果1件の「資料出力」）に **2ページ目「所在地・アクセス」** を追加。
  住所・最寄り・地図（幅185mm×高さ116mm、赤丸＝施設位置）を印刷。1ページ目（費用）のレイアウトは変更なし
- 印刷用の地図は Leaflet ではなく `KaigoPrint.staticMapHTML()`（国土地理院タイル画像をそのまま並べて中央にピン）。
  対話地図は印刷時にタイルの読み込み待ち・欠けが起きやすいため。無料・APIキー不要・従量課金なし
- 施設詳細ページをブラウザからそのまま印刷した場合も、対話地図の代わりに同じ静的地図（`.print-only`）を出す
- 座標が引けない施設（現在0件）は地図ページを出さない。住所を変えたら `tools/build_geo_data.py` で座標を再生成すること
- 2件以上の比較表（資料出力）には地図なし（変更なし）

### 2026-09-18（v2.15.2）

**駅名で検索中に「月額が安い順」が効かない不具合を修正**（兵頭さん指摘「安い順が違って出てきたケースがあった」）。
原因: 駅名を入れると結果を常に「徒歩分数の近い順」で並べ直していたため、並べ替えで「月額が安い順／高い順」を
選んでいても徒歩順で上書きされていた（例: 伊丹＋安い順 → 37.5万 → 38.5万 → 10.1万 …）。駅名なしのときは正常。
- 駅名検索中でも「月額が安い順／高い順」を選んでいればその順を保つ。徒歩順になるのは並べ替えが
  「更新が新しい順」（初期値）か、新設の「最寄駅から近い順」のときだけ
- 並べ替えに「最寄駅から近い順（駅名で検索したとき）」を追加（駅名なしのときは更新順と同じ）
- 補足（仕様どおり・変更なし）: 安い順は表示している「月々の費用（目安・すべて含む）」の下限で並ぶ。
  種別ごとに上乗せ額が違う（老健 +2.7万／介護付 +2.9万／住宅型・サ高住・特養 +3.0万／グループホーム +3.6万）ため、
  カード下段の「施設費用」だけを見ると前後が入れ替わって見える箇所がある（1,168件中203か所、老健と特養の境目など）

### 2026-09-16（v2.15.1）

**管理画面「検索履歴」の集計が50件で頭打ちになる不具合を修正**（兵頭さん指摘「50が上限？」）。
原因: 一覧用に最新50件だけ取得し、その50件から総検索回数・ユニークユーザー・本日回数を計算していた。
- 総検索回数・本日の検索回数: Firestore の件数集計クエリ（`count()`）で全件を正確に数える
- ユニークユーザー数・よく使われる条件: 直近1,000件から集計（1,000件を超えたら「直近1,000件中」と明記）
- 履歴一覧: 50件ずつ表示し「さらに50件を表示」で直近1,000件まで遡れる

### 2026-09-16（v2.15）

**検索結果の月額を「月々の費用（目安・すべて含む）」に変更＋費用不明施設の削除**（兵頭さん依頼）。
- 検索カードの月額は、施設費用（賃料＋管理費＋食費＋その他）に種別ごとの介護保険自己負担・医療費・
  生活費の目安レンジを加えた「約 X〜Y 万円」に。施設詳細ページ・資料出力と同じ `KaigoCost.get(種別)` で計算
  （例: 春日丘荘グループホーム 9.5万円 → 13.1〜16.1万円）。カード下段に「施設費用 9.5万円 ＋ …の目安」と内訳の出どころを明記
- 並び替え（月額が安い順／高い順）と価格フィルター（下限・上限）も表示と同じ目安の**下限値**で判定
- 費用が不明だった「医療法人社団緑水会介護老人保健施設シルバーハウス高槻」を Firestore から削除
  （削除前に `facilities-deleted-<id>-<日時>.json` へ退避。`tools/kaigo_delete_facility.py`）。計1,168件

### 2026-09-15（v2.14）

**施設一覧の静的ファイル化＝検索時のFirestore読み取りをゼロに**（無料プラン Spark の1日5万読み取り上限への根本対策）。
- `tools/build_facilities_json.py` が本番 Firestore から `public/kaigo-facilities.json`（施設ID付き）を生成し、
  Hosting で配信（gzip圧縮）。検索ページは `loadFacilitiesStatic()` でこれを最優先に読む
  （`cache:'no-cache'` でETag確認＝更新が無ければ転送なし）
- ファイルが取れない場合のみ v2.13 の Firestore 経路（キャッシュ即表示＋差分同期）に自動で切り替え
- 施設詳細ページは従来どおり Firestore を1件だけ読む（IDは一覧ファイルに含めているためリンクは不変）
- 運用: データを変えたら再生成＋デプロイ。`run_add_area.sh` は verify 後に自動で再生成する。
  管理画面・施設側で個別に変更した内容は再生成＋デプロイまで検索一覧に反映されない（現状、空室は
  「要問い合わせ」表示のため影響なし。施設の自己更新が始まったら差分同期との併用に拡張する）
- 効果: 病院数に関係なく無料枠内。検索の初回表示も高速化（一覧ファイルは圧縮後100KB前後）

### 2026-09-14（v2.13）

**検索の読み込みを高速化・読み取り課金を削減（A＋B）**。
- A: Firestore の端末内キャッシュ（`enablePersistence`、IndexedDB）を有効化し、施設一覧は
  `onSnapshot` で「キャッシュ即表示 → サーバーと差分同期して確定表示」。施設詳細から戻ったとき・
  翌日以降の初回表示が即時になり、サーバー読み取りは変更分だけになる（Firestore の仕様で
  30分以上間が空いた再接続は全件分の読み取り扱い）。非対応環境では従来どおり毎回取得。
- B: 5分以内の再検索は取り直さない（`DATA_TTL_MS`）。絞り込みは全て端末側なので結果は同じ。
- サーバー無応答時は15秒でキャッシュ表示のまま確定、キャッシュも無ければ従来のエラー表示。
- 無料枠（1日5万読み取り）の目安: 端末ごとに初回1,169件、以降は差分のみ。病院20端末でも初日2.3万件で枠内。
  根本対策（読み取りゼロ）は一覧の静的ファイル化（Firestoreバンドル）で、必要になったら追加する。

### 2026-09-14（v2.12.1）

**パスワードを忘れた病院への対応手順を整備**。病院IDのメールはダミー（`{loginId}@meets-medical.jp`）で
実在しないため再設定メール方式は成立しない。
- 管理者用初期化ツール `tools/kaigo_reset_password.py`（`find` で対象を探し `reset` で初期パスワード＝IDに戻す）
- ログイン画面のエラー文言に「初期パスワードはIDと同じ文字列です」を追加し、フォーム下に「忘れた場合は
  Meets Medical へ連絡→管理者が初期化」の案内を掲載
- 病院は自分でパスワードを変えられる（担当者管理 →「自分のパスワードを変更」。現在のパスワードが必要）。
  忘れた場合のみ管理者初期化。連絡経路: 病院 → 兵頭さん → 小池さん（数分で対応）

### 2026-09-14（v2.12）

**駅検索を「登録最寄駅の完全一致」に戻す**（兵頭さん要望「エクセルで伊丹駅と書いているものだけ表示したい。281件も出ないはず」）。
v2.6で入れた距離圏（駅から一定距離）判定を廃止。入力駅名と `stations[].name` が完全一致する施設だけを
徒歩分数の近い順に表示（「新伊丹」など似た名前の別駅は含めない。伊丹駅＝52件）。完全一致が全体で0件のときだけ
部分一致に落とす（「茨木」→JR茨木・茨木市・南茨木。0件を避けるため）。登録に無い駅名はフリー検索として動く。
駅マスター（kaigo-eki-data.js）の読み込みを検索ページから外した（座標・地図は施設詳細で引き続き使用）。
カードの最寄駅表記は徒歩分数がある場合のみ「〇〇駅 徒歩N分」、バス便などはデータどおり（例: 伊丹 バス約8分）。

### 2026-09-14（v2.11.2）

絞り込みチップの「最寄駅: 〇〇駅（車15分圏）」から「（車15分圏）」を削除し「最寄駅: 〇〇駅」に（兵頭さん要望）。
判定ロジック（駅から一定圏内を近い順）は変更なし。これで画面上の「車」表記はゼロ。

### 2026-09-13（v2.11.1）

**配布URLの基点を正式ドメインに修正＋文書内URLの統一**。
`kaigo-master.html`（管理画面のQR・施設URL・更新URL）と `kaigo-register.html`（登録時QR）の基点定数が
廃止済みの `kaigo-meetsmedical.surge.sh`（2026-09-07 停止）のままで、**それ以降に発行したQR・URLは
リンク切れだった**。`https://kaigo.meetsmedical.com` に固定（管理者がどのホストで開いても正式URLで発行）。
あわせて仕様書冒頭・変更要件まとめ・deploy.sh・納品書v1/v2・ID発行納品書の公開URLを正式ドメインに統一。
残した旧URL参照は「1-2 ドメイン」のCNAME向き先と旧URL並行運用の記述のみ（技術的事実）。
リポジトリ直下の `kaigo-guide-*.html`／`kaigo-workspace.html` の surge リンクは旧モック（別リポジトリ）向けの
遺物で本番とは無関係のため対象外。

### 2026-09-13（v2.11）

**正式URL `kaigo.meetsmedical.com` を開通**。Firebase Hosting カスタムドメイン＋Google Trust Services 証明書。
旧URL（web.app）は並行運用。詳細は「1-2. 公開URL・ドメイン」。

### 2026-09-11（v2.10）

**大阪5エリア（西淀川区・摂津市・高槻市・東淀川区・淀川区）322件を追加＋「車で〇分」表示を廃止**（計1,169件）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | データ追加 | 西淀川区44・摂津市33・高槻市124・東淀川区78・淀川区43（`tools/build_import_data_260911b.py`→`kaigo-import-data-260911b.js`）。既存847件との名前＋住所の一致ゼロ、駅マスター未収載ゼロ |
| 2 | 大阪市の区の扱い | `city` は「大阪市西淀川区」のように市＋区のフル表記（区ごとに絞り込めるように）。CITY_MAP・mapDoc市判定にも同表記で追加し、判定リストでは区名を「大阪市」より前に置く |
| 3 | カード表示の修正 | 駅検索時に出していた推定「車で約N分」を廃止し、施設データの最寄駅表記（徒歩約N分／バス約N分）をそのまま表示（兵頭さん指摘：さくらヴィラは「バス約3分」なのに「車で4分」と出ていた）。入力欄ヒントも「駅の周辺（約15分圏）」表現に |
| 4 | 同一施設の判定 | `kaigo_add_area.py` の二重取り込み判定を名前のみ→名前＋住所に厳密化。同名別施設「グループホームひより」（豊中／高槻）を正しく別施設として追加 |
| 5 | 座標ビルダー | `build_geo_data.py` に既存座標の再利用キャッシュ（毎回全件取り直しで6分超・失敗しやすかったのを、新規住所のみ取得に）。1,121住所（再利用821・新規300・失敗0） |

**元Excelの整合警告2件**：グループホームみのり（月額合計が内訳より350円多い）・大冠カームグループホーム（同500円）。
システムは内訳（家賃・食費・管理費）を保存し合計は計算表示するため取り込みには影響しない。提供元に確認事項として伝達。

検証（実データ1,169件・スタブ環境）：市区町村14（大阪10＋兵庫4）・市別件数一致・新エリア322件の座標欠けゼロ。
駅検索は高槻駅141件（高槻124＋茨木17）・十三193件（豊中・淀川区・西淀川区・尼崎の府県またぎ）・千里丘217件、
いずれも距離順で「車で」表示なし。区の施設（大阪市淀川区）の詳細ページ地図も確認。

**本番反映の順序**：尼崎（v2.9・191件）が未適用のため、①尼崎の apply → ②本エリアの backup→dryrun→apply→verify →
③両ブランチを main にマージ → ④ホスティングをデプロイ。取り込みスクリプトは対象市に既存ドキュメントがあれば
自動中止するため、順序を誤っても二重取り込みにはならない。

### 2026-09-11（v2.9）

**尼崎市191件を追加**（計847件）。手順はv2.8で確立したエリア追加フローの2回目。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | データ追加 | 尼崎市191件（`tools/build_import_data_260911.py`→`kaigo-import-data-260911.js`）。金額整合・駅表記・重複・既存656件との同名衝突すべてゼロ確認 |
| 2 | CITY_MAP | 兵庫県に尼崎市を追加（尼崎・伊丹・川西・宝塚の4市に） |
| 3 | 取り込みスクリプトの汎用化 | `tools/kaigo_add_area.py` 新設。backup→dryrun→apply→verify の4段・追加専用（更新/削除のコードパスなし）・対象市/件数はデータファイルから自動導出。次のエリア追加からはこれ1本 |
| 4 | 座標再生成 | 821住所（＋尼崎185）。駅マスターは尼崎の登録13駅すべて収載済みのため変更なし |

検証（実データ847件）：市区町村9市・市別件数一致・尼崎の座標欠けゼロ。駅検索は武庫之荘215件・
塚口299件（JR/阪急の同名2駅を統合し、尼崎・伊丹・豊中と県境をまたいで表示）。尼崎施設の詳細ページ地図も確認。

### 2026-09-09（v2.8）

**兵庫3市（伊丹市・川西市・宝塚市）180件を追加＋駅座標を静的マスター化**。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | エリア追加 | 伊丹市70件・川西市42件・宝塚市68件（Excel 3ファイル→`tools/build_import_data_260909.py`→`kaigo-import-data-260909.js`）。取り込みは `tools/kaigo-xlsx-import-260909.html`（**安全マージ専用**。照合を兵庫3市の既存ドキュメントに限定し、大阪476件には一切触れない） |
| 2 | 表示エリアの拡張 | 暫定フラグ `OSAKA_ONLY` を廃止し `SHOWN_PREFS = ['大阪府','兵庫県']` 方式に。府県を増やすときは SHOWN_PREFS と CITY_MAP に追記するだけ。CITY_MAP の兵庫県はデータのある3市のみ列挙 |
| 3 | 駅座標の静的マスター化 | HeartRails API が502で全面停止（2026-09-09）し円域判定が保険動作に落ちていたため、OpenStreetMap由来の近畿4府県1,150駅マスター（`kaigo-eki-data.js`）を同梱。同期参照になり応答待ちも解消。異体字・通称の別名対応（JR茨木・鶯の森）。API はマスター未収載の駅名だけの保険に降格 |
| 4 | 座標データの統合生成 | `build_geo_data.py` を全 `kaigo-import-data-*.js` 統合方式に修正（最新ファイルだけ読むと他エリアの座標が消える問題を予防）。636住所（大阪463＋兵庫173）を収録 |

**検証（実データ656件）**：都道府県=大阪府・兵庫県／市区町村8市／伊丹市70・川西市42・宝塚市68。
駅検索は伊丹駅188件（県境をまたいで豊中・池田の圏内施設も表示＝円域判定の本領）・川西能勢口147件・
鶯の森（異体字別名）96件・豊中232件。兵庫施設の詳細ページ地図・費用表示も確認。名前衝突（大阪の同名施設）ゼロ確認済み。

**本番反映の順序**：①管理者が `kaigo-xlsx-import-260909.html` で Firestore に180件を取り込み →
②ホスティングをデプロイ。どちらが先でも壊れないが（コード先行なら兵庫が0件表示、データ先行なら
旧コードのフィルタで非表示になるだけ）、この順が自然。

### 2026-08-25（v2.7.3）

**スマホ表示（レスポンシブ）を修正**。検索後の画面が2カラムのまま潰れていた問題を解消
（720px以下対象、`kaigo-search.html` のみ）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 検索後は縦積みに | サイドバーを畳んで結果一覧を全幅表示。上部の「検索条件を変更する」ボタンで条件を開閉（再検索すると自動で閉じる） |
| 2 | ソートバーの折り返し | 件数・表示件数・選択解除・並べ替えを折り返し配置にし、ボタンが潰れないように |
| 3 | 費用欄の縦積み | カード内の入居時費用/月額パネルを縦に並べ、賃料・管理費・食費が読める幅を確保 |
| 4 | ヘッダーの圧縮 | ボタン余白・文字サイズを縮小し1行に収まるように。担当者名は長い場合省略表示 |

検証（375px幅・実データ476件）：初期画面・検索結果・チェック＋一括解除・条件の開閉・
施設詳細（地図タイル読込含む）のすべてで横スクロール／はみ出し要素なしを確認。
PC表示は720pxより上のため影響なし。

### 2026-08-25（v2.7.2）

**検索結果に「選択をすべて解除」ボタンを追加**（兵頭さん依頼）。資料出力用に施設をチェックした後、
1件ずつ外さなくても一括で解除できるように、結果一覧上部（件数表示の隣）に解除ボタンを設置。
選択が1件以上あるときだけ「選択をすべて解除（N件）」と表示され、押すと表示中ページ外の選択も
含めて全部解除される（右下の資料出力ボタンも連動して消える）。`kaigo-search.html` のみ。

### 2026-08-25（v2.7.1）

**ヘッダーの担当者名表示の二重敬称を修正**（兵頭さん指摘）。担当者名を「渕上様」のように
敬称つきで登録すると「渕上様 さん」と表示されていた。名前の末尾に敬称（様・さま・さん・先生）が
既に付いている場合は「さん」を足さないよう修正（`kaigo-search.html` のヘッダーラベル1箇所）。

### 2026-08-24（v2.7）

**検索窓を1つに統合**（兵頭さん要望「検索窓は1つがいい」「フリー検索に豊中駅と入れたら出るように」）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 「最寄駅から検索」欄を廃止 | フリー検索窓1つに統合。ラベルは「フリー検索（施設名・駅名・住所など）」のまま |
| 2 | 駅名の自動判定 | 入力が駅名（登録駅と完全一致 or 駅APIで該当）なら自動で車15分圏検索に切替。「豊中駅」でも「豊中」でもOK |
| 3 | モードの可視化 | 絞り込みチップで「最寄駅: 豊中駅（車15分圏）」/「検索: 〇〇」を出し分け |
| 4 | 互換対応 | 2窓時代に保存した検索条件も読み込める |

**背景**：v2.6まで「豊中駅」とフリー検索に入れても0件だった（内部の駅名が「豊中」表記のため
「駅」付きでは部分一致しない）。統合により、どちらの窓に入れるか迷う問題と「駅」表記ゆれの
問題が同時に解消した。検証：豊中駅/豊中=駅モード214件・豊中市=フリー154件・江坂=駅モード
197件・さくら=フリー13件・存在しない駅名=フリー0件（エラーなし）。

### 2026-08-24（v2.6）

**最寄駅検索を「車で約15分圏」に拡張＋施設詳細に所在地地図**（兵頭さん依頼）。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 車15分圏の距離検索 | 駅名から座標を引き（HeartRails API）、施設座標（事前ジオコーディング）との直線距離で判定。登録最寄駅に関係なく圏内の施設をすべて表示。「豊中駅」17件 → 214件 |
| 2 | 未登録駅にも対応 | データに最寄駅として登録が無い駅（例: 江坂）でも検索可能に |
| 3 | 施設座標データ | `public/kaigo-geo-data.js` を新設（476件中463住所、国土地理院APIで生成）。生成スクリプト `tools/build_geo_data.py` |
| 4 | カード表示 | 徒歩分数があれば「徒歩N分」、無ければ直線距離からの推定で「車で約N分」 |
| 5 | 施設詳細に地図 | Leaflet＋国土地理院タイル（無料・キー不要・従量課金なし）＋Googleマップ経路リンク |

**動作のポイント（誤解しやすい点）**：施設側に最寄駅の登録があるかどうかは判定に関係ない。
駅名を入れれば「車で約15分圏内」の施設は登録の有無にかかわらずすべて表示される。
駅側も同様で、どの施設にも登録されていない駅名（例: 江坂）を指定できる。

**検証実績（2026-08-24・実データ476件で確認）**

| 検索 | 結果 | 備考 |
|------|------|------|
| 豊中駅 / 豊中（表記ゆれ） | 214件・同一結果 | うち登録最寄駅の一致は17件のみ。残り197件は距離だけでヒット |
| 江坂（施設データに登録なしの駅） | 197件 | 吹田市・豊中市の圏内施設が距離順に表示 |
| 梅田（対象5市の外の駅） | 8件 | 豊中市南部の圏内分のみ。距離判定が正しく効いている |
| 存在しない駅名 | 0件 | エラーにならずフォールバック動作 |
| 距離順ソート | 0.13km→4.79km | 上限（直線約4.8km）内で近い順を確認 |

**費用について**：地図・座標・駅APIはすべて無料構成（国土地理院タイル・国土地理院ジオコーディング・
HeartRails・Leaflet）。Google Maps API は使っていないため、アクセスが増えても従量課金は発生しない。

**精度の注意**：「車で約15分」は直線距離×係数1.3を時速25km/hで換算した推定値（渋滞・信号は考慮しない）。
実ルートが必要になったら Google Distance Matrix 等（有料）への差し替えを検討する。

### 2026-08-24（v2.5）

**最寄駅から検索を新設**（兵頭さん依頼）。対象は `public/kaigo-search.html` のみ。

| # | 変更点 | 内容 |
|---|--------|------|
| 1 | 「最寄駅から検索」入力欄を追加 | フリー検索の下に専用欄。「〇〇駅」でも「〇〇」でも同一の検索として扱う |
| 2 | 徒歩分数の近い順ソート | 検索した駅の徒歩分数が小さい施設から表示。分数未登録は末尾 |
| 3 | カードの最寄駅タグ切替 | 最寄駅検索中は「検索した駅＋徒歩分数」を表示 |
| 4 | 付帯対応 | 絞り込みチップ・検索条件保存/復元・詳細から戻ったときの結果復元・検索ログ（`nearestStation`）に対応 |

**背景**：従来はフリー検索しかなく、「豊中」と入れると豊中市（住所）と豊中駅（駅名）が混ざってヒットし、
駅起点で近い施設を探す使い方ができなかった。最寄駅検索は照合対象を駅データに限定し、距離順で返す。
詳細仕様は「5-1. 検索画面」の**最寄駅から検索**を参照。

### 2026-08-20（v2.4）

兵頭さんからの指摘3点に対応。対象は `public/` 配下の3ファイル。

| # | 変更点 | 変更前 → 変更後 | ファイル |
|---|--------|----------------|---------|
| 1 | **月額目安の表記を統一** | `8〜10万円`（整数に四捨五入）→ `8〜9.9万円`（小数第1位） | `kaigo-print.js` |
| 2 | 検索結果カードの選択チェックボックスを拡大 | 20px → 28px（白い台紙＋黒枠＋影を追加） | `kaigo-search.html` |
| 3 | 「印刷・PDF」ボタンを拡大 | 施設詳細：13px → 17px ／ 資料出力タブ：11px角ボタン → 17px丸ボタン＋画面右上に固定 | `kaigo-facility-view.html`, `kaigo-search.html`, `kaigo-print.js` |

**1の背景**：単票資料（`kaigo-print.js`）だけがヒーロー金額を `Math.round()` で整数に丸めており、9.9 → 10 に繰り上がっていた。施設詳細ページ・比較表は当初から小数第1位表示だったため、同じ施設で表記が食い違っていた。丸め処理を廃止し、桁数に応じてフォントサイズを可変（82pt / 68pt / 58pt）にしてA4幅に収まるようにした。

**3の補足**：資料シートの印刷ボタンは `position:fixed` に変更したため、長い比較表を下までスクロールしても常に押せる。ボタン拡大に伴い施設詳細ページのヘッダー高さを 58px → 68px に調整。
